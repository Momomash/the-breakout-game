export enum Theme {
    LIGHT = 'light',
    DARK = 'dark',
}

export enum Language {
    EN = 'en',
    RU = 'ru',
}

export enum Position {
    RIGHT= 'right',
    LEFT = 'left',
    TOP = 'top',
    BOTTOM = 'bottom',
}